image: node:12.11

pages:
  cache:
    paths:
      - node_modules/
  script:
    - npm install
    - npm run buildProd
    - cp file index.d.ts node_modules/tesseract.js/src/index.d.ts
  artifacts:
    paths:
      - public
  only:
    - master
    - pages